// import fs from 'fs/promises';
// import { getPaginatedAllCards } from '../api/hs-api';

// // Función para guardar datos en un archivo local
// export const saveCardsLocally = async () => {
//   const cards = await getPaginatedAllCards({ locale: 'esMX', collectible: 1 });
//   await fs.writeFile('cards.json', JSON.stringify(cards, null, 2));
// };


// saveCardsLocally();
